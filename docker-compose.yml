services:
  app:
    image: nextjs-json-test:latest
    ports:
      - "3000:3000"
    volumes:
      - ./outer-data/outer-version.json:/usr/src/app/outer-version.json
    environment:
      - NODE_ENV=production
